import React from 'react';

const WaterAnimation = ({children}:any) => {
    return (
            <div className="w-full h-[50px]   inset-0 bg-gradient-to-b from-blue-300 to-blue-500 animate-water opacity-50"/>
    );
};

export default WaterAnimation;